from fastapi import APIRouter

api_router = APIRouter()

# Route modules get registered here, e.g.:
# from app.api.v1.endpoints import accounts
# api_router.include_router(accounts.router, prefix="/accounts", tags=["accounts"])
